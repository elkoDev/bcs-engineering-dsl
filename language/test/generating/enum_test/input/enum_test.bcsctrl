// A simple enum test file to verify enum code generation
control ValidTestController {
    enum Mode {
        OFF, ECO, COMFORT
    }

    unit TestUnit {
        var currentMode: Mode = Mode.OFF;
        
        if (currentMode == Mode.ECO) {
            currentMode = Mode.COMFORT;
        }
    }
}